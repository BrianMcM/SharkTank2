SharkTank is a distributewd application for asynchronous video sharing that uses a series of distributed nodes to support fast and resilient video sharing.

To Run:

mvn package
docker compose up --build
